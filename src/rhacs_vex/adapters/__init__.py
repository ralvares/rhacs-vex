"""Scanner input adapters — map external scan reports onto the triage
DataFrame shape that rhacs_to_df produces, so the engine judges every scanner
the same way (scanner = discovery, engine = verdict)."""
