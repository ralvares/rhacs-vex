# Built CLI binary (~150 MB). Regenerate with the build step in README.
scannerctl

# Downloaded vulnerability bundle (~240 MB). Fetch with ./fetch-vuln-bundle.sh
vuln-data/

# TLS material — contains PRIVATE KEYS. Regenerate with ./gen-certs.sh
certs/

# Local scan output
*-sbom.json
*.scan.json
